const addHook = require('../hooks').addHookListener;

const settings = {
    name: 'Code Highlighter',
    description: 'Integrates HighlightJS class generation on the server-side of the post.',
    init: () => {
    }
};

module.exports = settings;
